name: Echo Test Variable

on:
  workflow_dispatch:  # This allows manual triggering

jobs:
  echo-variable-job:
    runs-on: ubuntu-latest
    environment: Development
    steps:
      - name: Echo test variable
        run: |
          echo "The value of test is: ${{ vars.ENVIRONMENT_STAGE }}"
